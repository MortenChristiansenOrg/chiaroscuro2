---
title: Colors
---

import { ColorSwatch, SwatchGrid } from "../components/ColorSwatch"

# Colors

All colors use `oklch(L C H / alpha)`. No hex, rgb, or hsl. The color system has two contexts: **glass** (browser chrome over the window background) and **content** (opaque panels for web content and settings). There is no dark mode — the glass chrome is always dark (light text on translucent dark), and content panels are always light.

## Glass Context (Chrome UI)

The chrome renders over a semi-transparent background that reveals the OS window acrylic effect, with `backdrop-filter: blur(var(--glass-backdrop-blur))`. All chrome colors are white or near-white at varying opacities.

<SwatchGrid>
  <ColorSwatch name="Glass Background" color="var(--glass-bg)" value="--glass-bg" />
  <ColorSwatch name="Hover Surface" color="var(--glass-hover)" value="--glass-hover" />
  <ColorSwatch name="Active Surface" color="var(--glass-active)" value="--glass-active" />
  <ColorSwatch name="Subtle Surface" color="var(--glass-subtle)" value="--glass-subtle" />
  <ColorSwatch name="Pressed Surface" color="var(--glass-pressed)" value="--glass-pressed" />
  <ColorSwatch name="Border" color="var(--glass-border)" value="--glass-border" />
</SwatchGrid>

## Text Colors (on glass)

<SwatchGrid>
  <ColorSwatch name="Primary Text" color="var(--glass-text-primary)" value="--glass-text-primary" />
  <ColorSwatch name="Default Text" color="var(--glass-text-default)" value="--glass-text-default" />
  <ColorSwatch name="Hover Text" color="var(--glass-text-hover)" value="--glass-text-hover" />
  <ColorSwatch name="Muted Text" color="var(--glass-text-muted)" value="--glass-text-muted" />
  <ColorSwatch name="Hint Text" color="var(--glass-text-hint)" value="--glass-text-hint" />
  <ColorSwatch name="Pressed Text" color="var(--glass-text-pressed)" value="--glass-text-pressed" />
</SwatchGrid>

## Content Context (Opaque Panels)

Inside content panels (`var(--content-bg)` background):

<SwatchGrid>
  <ColorSwatch name="Text Primary" color="var(--foreground)" value="--foreground" />
  <ColorSwatch name="Text Secondary" color="var(--content-text-secondary)" value="--content-text-secondary" />
  <ColorSwatch name="Text Muted" color="var(--content-text-muted)" value="--content-text-muted" />
  <ColorSwatch name="Border" color="var(--border)" value="--border" />
  <ColorSwatch name="Background" color="var(--content-bg)" value="--content-bg" />
</SwatchGrid>

## Workspace Accent Colors

Each workspace has a user-chosen accent color exposed as `var(--accent-hue)` — a single hue value. The chrome UI is deliberately achromatic so any accent harmonizes.

| Role | Value | Usage |
|------|-------|-------|
| Accent solid | `oklch(var(--accent-L) var(--accent-C) var(--accent-hue))` | Focus rings, active indicators |
| Accent muted | `oklch(var(--accent-L) var(--accent-C) var(--accent-hue) / 0.4)` | Workspace ring, subtle highlights |
| Accent glow | `oklch(var(--accent-L) var(--accent-C) var(--accent-hue) / 0.8)` | Loading gradient, animated borders |

### Curated Accent Palette

Users pick from a predefined set of hues. All share `L=0.65, C=0.15` for consistent vibrancy and accessibility.

<SwatchGrid>
  <ColorSwatch name="Iris (270)" color="oklch(0.65 0.15 270)" />
  <ColorSwatch name="Ocean (230)" color="oklch(0.65 0.15 230)" />
  <ColorSwatch name="Teal (195)" color="oklch(0.65 0.15 195)" />
  <ColorSwatch name="Jade (160)" color="oklch(0.65 0.15 160)" />
  <ColorSwatch name="Lime (130)" color="oklch(0.65 0.15 130)" />
  <ColorSwatch name="Amber (85)" color="oklch(0.65 0.15 85)" />
  <ColorSwatch name="Coral (45)" color="oklch(0.65 0.15 45)" />
  <ColorSwatch name="Rose (350)" color="oklch(0.65 0.15 350)" />
  <ColorSwatch name="Berry (320)" color="oklch(0.65 0.15 320)" />
</SwatchGrid>

## Semantic Colors

Only two non-accent semantic colors exist. Both are fixed — they don't shift per workspace.

<SwatchGrid>
  <ColorSwatch name="Destructive" color="var(--destructive)" value="--destructive" />
  <ColorSwatch name="Warning" color="var(--color-warning)" value="--color-warning" />
</SwatchGrid>

## Window Background

The window background is semi-transparent with no gradient, allowing the OS acrylic/vibrancy effect to show through:

```css
background: var(--window-bg);
```

## Rules

1. **No new colors without a role.** Every color must map to a role in the tables above. If you need a color that doesn't fit, add the role first.
2. **Opacity over new lightness.** In the glass context, adjust opacity of white rather than introducing new lightness values. This ensures the acrylic effect bleeds through consistently.
3. **3-state text brightness.** Interactive text uses three states: default (`--glass-text-default`) → hover (`--glass-text-hover`) → active (`--glass-text-primary`). This progression must be consistent across all glass UI text.
4. **Window background is not a design surface.** Never place text directly on the window background. All text must be on a glass or content surface.
5. **Color is accent-only.** The chrome UI is achromatic by default — white text, white/transparent backgrounds, no hue. Color appears only through the accent system or the destructive semantic.
6. **Never hardcode accent hues.** Always derive accent colors from `var(--accent-hue)`.
7. **Accent sparingly.** Use accent for focus rings, active workspace indicators, loading animations, and primary action buttons. Don't use it for backgrounds, text, or borders in normal states.
8. **No dark mode.** Single appearance. Glass chrome is dark by nature. Content panels are always light.
