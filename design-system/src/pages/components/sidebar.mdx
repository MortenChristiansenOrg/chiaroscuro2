---
title: Sidebar
---

# Sidebar

The sidebar is the primary navigation surface. It contains pinned tabs (icon strip), bookmarked tabs, folder groups, ephemeral tabs, and the workspace bar.

## Structure (top to bottom)

1. **Pinned strip** — horizontal row of favicon-only buttons
2. **"Bookmarked" label** — section header
3. **Tab list** — bookmarked tabs with optional folders
4. **Ephemeral divider** — line + "Clear" button
5. **Ephemeral tabs** — dimmer styling
6. **Workspace bar** — workspace bubbles + edit/add controls (anchored to bottom)

## Container Specs

| Property | Value |
|----------|-------|
| Width | `230px` |
| Padding | `5px 0 0` |
| Background | Transparent (inherits glass body) |
| Overflow | `overflow-y: auto` |

## Section Labels

| Property | Value |
|----------|-------|
| Font size | `9px` |
| Transform | `uppercase` |
| Letter spacing | `0.1em` |
| Font weight | `500` |
| Color | `oklch(1 0 0 / 0.3)` |
| Padding | `10px 14px 3px` |

## Folder Headers

| Property | Value |
|----------|-------|
| Font size | `11px` |
| Font weight | `500` |
| Color | `oklch(1 0 0 / 0.35)` |
| Closed color | `oklch(1 0 0 / 0.25)` |
| Padding | `4px 14px` |
| Margin | `0 6px` |
| Gap | `7px` (icon + text) |
| Icons | ChevDown/ChevRight + FolderOpen/Folder |

## Ephemeral Divider

A horizontal line with a "Clear" button at the right end:

| Property | Value |
|----------|-------|
| Line | `1px` height, `oklch(1 0 0 / 0.07)` |
| Container padding | `6px 8px 4px 14px` |
| Clear button | `9px`, weight `500`, `oklch(1 0 0 / 0.25)` |

## Separator Lines

Between sections: `height: 1px`, `oklch(1 0 0 / 0.07)`, `margin: 4px 14px`.
