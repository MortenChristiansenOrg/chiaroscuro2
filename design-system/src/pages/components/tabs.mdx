---
title: Tabs
---

import { ComponentPreview } from "../../components/ComponentPreview"

# Tabs

Pill-shaped sidebar tabs. Each is a list item with favicon, label, and close button.

## Pattern

- Full-width click target with `0.25rem` vertical padding and `0.75rem` horizontal padding.
- Pill-shaped border radius (`--radius-pill`).
- Favicon at `1rem × 1rem`, circular clip.
- `0.625rem` gap between icon and label.
- Single line of text, truncated with ellipsis on overflow.
- Follows the [state ladder](/interaction).

## States

<ComponentPreview label="Tab States (dark bg)">
  <div style={{ background: "oklch(0.35 0.06 250)", borderRadius: 12, padding: 12, width: 230, fontFamily: "'Plus Jakarta Sans', sans-serif" }}>
    {[
      { active: false, hover: false, label: "Default" },
      { active: false, hover: true, label: "Hovered" },
      { active: true, hover: false, label: "Active" },
    ].map((state) => (
      <div
        key={state.label}
        style={{
          display: "flex", alignItems: "center", gap: 10,
          padding: "0.25rem 0.75rem", margin: "1px 0.375rem", borderRadius: 20,
          background: state.active ? "oklch(1 0 0 / 0.22)" : state.hover ? "oklch(1 0 0 / 0.1)" : "none",
          boxShadow: state.active ? "0 1px 4px oklch(0 0 0 / 0.05)" : "none",
          cursor: "pointer",
        }}
      >
        <div style={{ width: 16, height: 16, borderRadius: "50%", background: "oklch(0.22 0 0)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 8, fontWeight: 600, color: "#fff" }}>G</div>
        <span style={{ fontSize: '0.75rem', color: state.active ? "oklch(1 0 0 / 0.95)" : state.hover ? "oklch(1 0 0 / 0.75)" : "oklch(1 0 0 / 0.55)", fontWeight: state.active ? 500 : 400, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
          {state.label} — GitHub
        </span>
      </div>
    ))}
  </div>
</ComponentPreview>

## State Tokens

| State | Background | Text opacity | Weight | Shadow |
|-------|-----------|-------------|--------|--------|
| Default | transparent | `0.55` | 400 | none |
| Hover | `oklch(1 0 0 / 0.1)` | `0.75` | 400 | none |
| Active | `oklch(1 0 0 / 0.22)` | `0.95` | 500 | Subtle |
| Disabled | transparent | `0.25` | 400 | none |

## Tab Types

Three tab types exist with different default text brightness:

| Type | Default text opacity | Behavior |
|------|---------------------|----------|
| Pinned | `0.55` | Global, always visible as favicon-only |
| Bookmarked | `0.55` | Per-workspace, in sidebar list |
| Ephemeral | `0.4` | Auto-expire after 8hr TTL |

## Close Button

Hidden by default, visible on parent hover. Shifts to destructive color on its own hover. See [Buttons](/components/buttons).
