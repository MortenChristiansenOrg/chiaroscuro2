---
title: Cards
---

import { ComponentPreview } from "../../components/ComponentPreview"

# Cards

Content cards in the content panel. Each has an image placeholder, title, and description.

<ComponentPreview label="Card">
  <div style={{ maxWidth: 240 }}>
    <div style={{
      borderRadius: "var(--radius-lg)", overflow: "hidden",
      border: "1px solid var(--border)",
      cursor: "pointer", fontFamily: "var(--font-sans)",
    }}>
      <div style={{ height: 120, background: "linear-gradient(135deg, oklch(0.92 0.02 200), oklch(0.88 0.03 250))" }} />
      <div style={{ padding: "var(--text-md) 1rem" }}>
        <h3 style={{ fontSize: 'var(--text-md)', fontWeight: 500, marginBottom: 4, letterSpacing: '-0.01em' }}>Light & Shadow</h3>
        <p style={{ fontSize: 'var(--text-base)', color: "var(--content-text-secondary)", lineHeight: 1.6, margin: 0 }}>Exploring the interplay of illumination and darkness.</p>
      </div>
    </div>
  </div>
</ComponentPreview>

## Specs

| Property | Token/Value |
|----------|-------------|
| Border radius | `--radius-lg` |
| Border | `1px solid var(--border)` |
| Title | `--text-md`, weight 500 |
| Description | `--text-base`, `var(--content-text-secondary)`, `line-height: 1.6` |

## Hover State

| Property | Value |
|----------|-------|
| Shadow | `var(--shadow-elevated)` |
| Transform | `translateY(-0.0625rem)` (1px max hover lift) |
| Transition | `transform var(--duration-fast) var(--ease-out), box-shadow var(--duration-fast) var(--ease-out)` |
